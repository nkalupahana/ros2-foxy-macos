name: code testing

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: macos-latest
    steps:
      - name: Download workflow artifact
        uses: dawidd6/action-download-artifact@v2.14.0
        with:
          workflow: "ros2-foxy-base.yml"
          workflow_conclusion: success   
      - name: Test
        run: ls

          
