name: code testing

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: macos-latest
    steps:
      - name: Test
        run: touch test.txt
      - uses: actions/upload-artifact@v2
        with:
          name: testfile
          path: test.txt
          
